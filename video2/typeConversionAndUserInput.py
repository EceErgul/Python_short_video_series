# TYPE CONVERSION (CASTING)

number_text = "42"
number = int(number_text)
print(number + 8)

# int() to convert data into an integer
# float() to convert data into a float
# str() to convert data into a string
# Bool() to convert data into a boolean (True or False)

# USER INPUT

name = input("What is your name: ")
print("Hello ", name)

age = input("How old are you: ")
print("You will be", int(age) + 1, "years old next year.")

# Do not Forget that input() always return a string.